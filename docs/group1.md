# 19:06 дата-создания документа
задачи в ближайщего дня:
# Дни 1–2. Проектирование и каркас:
Костя: делает архитектуру проекта<br>
Марина: делает основу хендлеров, ```healthpoint(проверка жизни способности)```. скидывает мне(если костя не успел сделать архитектуру)
# Дни 3–4. Группы и студенты (без транзакции):
Саша: делает рабочий эндпоинт ```POST,GET /groups```,```GET /students(фильтр по student_id)```. без транкзакции. обязательная валидация
# Дни 5–6. Транзакция (ядро практики):
Заур: ```POST /students``` создания дефолтных задач и студента, ```GET /students/{id}/tasks```. валидация, транкзакция<br>

# День 7. Обновление статуса и причесывание ошибок:
Астемир: PATCH /tasks/{id} с валидацией статуса (oneof), сведение всех ошибок к sentinel + единому JSON, проверка корректности HTTP-статусов (404 на несуществующий id и т.д.). Базовое логирование запросов и ошибок. При наличии вопросов по sentinel уточнить у преподавателя.
# День 8:
Костя: сделать документацию и curl запросы, api

# День 9:
Костя и Марина: смотреть как они мучуються и перейти на пересдачу( хрен я буду выполнять их работу )

> [!IMPORTANT]
> Остальные дни по этапу разработки